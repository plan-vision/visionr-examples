Build a VisionR demo app in this empty generated project for managing students, courses, and departments.

Departments can be nested. Courses belong to departments and have multiple participating students, without a separate enrollment object unless the relationship needs its own data. Add useful reverse relations, hierarchy navigation by
department, document upload support, table columns, i18n labels, sample data, and a main Courses page using common-main.

Add a verify-project task that checks the imported data, including that Applied Data Modeling has two participants.
